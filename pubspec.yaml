name: movie_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  tmdb_api: ^2.2.0
  dio: ^5.4.3+1
  provider: ^6.1.2
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  cache_systems: ^0.0.2
  like_button: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
